// Capacitor utilities - web-only stubs
// Native Capacitor features are only available in native builds

export const initializeApp = async () => {
  // No-op in web environment
};

export const isNative = () => false;
export const getPlatform = () => 'web';
